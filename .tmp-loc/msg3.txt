fix(ci): sync the lockfile with the three websites that no longer take @duncit/i18n

The previous commit added a per-site i18n wrapper to ads/earnwith/partners, then
deleted them again once the shared @duncit/brand one made them redundant — but
the dependency came out of the three package.json files after the last install,
so pnpm-lock.yaml still listed it.

Every deploy job installs with --frozen-lockfile, which is why all of them died
in four seconds at the install step, before the typecheck they are named for.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
