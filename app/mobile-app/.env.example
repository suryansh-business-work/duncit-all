# Staging web export / build — points at the staging API. Loaded when
# bundling with `--mode staging`; CI passes EXPO_PUBLIC_API_URL as a build-arg.
EXPO_PUBLIC_API_URL=https://staging.server.duncit.com
EXPO_PUBLIC_GOOGLE_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_MAP_API=
EXPO_TOKEN=
