export { DuncitDialog } from './DuncitDialog';
export type { DuncitDialogProps } from './DuncitDialog';
export { DialogHeader } from './DialogHeader';
// The sizing contract lives in @duncit/dialogs-native so its rules are tested
// once and stated in one place; only the Tamagui view below is app-local.
export { dialogMetrics } from '@duncit/dialogs-native';
export type { DialogMetrics, DialogMetricsInput, DuncitDialogVariant } from '@duncit/dialogs-native';
