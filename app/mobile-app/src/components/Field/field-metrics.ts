/**
 * The box every labelled control draws — text inputs, the select trigger —
 * so a form's fields line up at one height and one corner (rule 34). The calm
 * design's input: ~52 tall, 14px corners, surface fill, hairline border.
 */
export const FIELD_HEIGHT = 52;
export const FIELD_RADIUS = 14;
