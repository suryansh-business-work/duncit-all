export interface BlockSlotFormValues {
  template_id: string;
  from: string;
  to: string;
  reason: string;
}

export interface BlockVenueTimeslotInput {
  template_id: string | null;
  from: string;
  to: string;
  reason: string;
}
