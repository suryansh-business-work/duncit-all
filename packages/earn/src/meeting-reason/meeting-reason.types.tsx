/** Shared types for the onboarding-meeting reschedule / cancel reason form. */

export interface MeetingReasonValues {
  reason: string;
}

export const blankMeetingReasonValues: MeetingReasonValues = {
  reason: '',
};
