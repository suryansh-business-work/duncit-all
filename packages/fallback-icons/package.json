{
  "name": "@duncit/fallback-icons",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The canonical fallback-icon contract. Names every project must ship locally, so a server icon that fails to load always has a bundled replacement — and a missing one fails the build. Zero runtime deps.",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "react-native": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.4.5",
    "vitest": "^2.1.9"
  }
}
