// Types generated from the server GraphQL schema (see server/codegen.ts →
// packages/gql-types/src/schema.ts). Import these to type test mocks so the
// mock shape stays in sync with the schema — a schema change that breaks a
// mock surfaces as a typecheck error, not a silent runtime drift.
export * from './schema';
