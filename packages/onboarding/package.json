{
  "name": "@duncit/onboarding",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shared Earn With Duncit onboarding logic — the four journeys and the card state machine. Zero runtime deps so mWeb, native and the portals consume one implementation.",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "react-native": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "e2e": "node ../../scripts/e2e-no-surface.mjs @duncit/onboarding"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.4.5",
    "vitest": "^2.1.9"
  }
}
