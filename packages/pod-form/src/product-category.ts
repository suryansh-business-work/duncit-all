/** Re-exported from @duncit/utils so pod editors and the standalone apps share
 * one implementation of the club ⇄ product category match (Super + Sub). */
export {
  clubCategoryKey,
  productMatchesClub,
  filterProductsForClub,
  type ClubCategoryKey,
} from '@duncit/utils';
