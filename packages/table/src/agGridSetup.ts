import { AllCommunityModule, ModuleRegistry } from 'ag-grid-community';

// Registered once at package module scope — every DuncitTable shares this.
ModuleRegistry.registerModules([AllCommunityModule]);
