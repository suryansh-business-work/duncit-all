---
name: '@duncit/utils'
summary: 'Framework-free browser and money helpers — API error parsing, INR formatting, file download/read, pod spot economics and the shared earnings statement.'
category: 'platform'
zeroDeps: true
frameworkFree: true
coverageGate: true
consumers:
  - 'app/mweb'
  - 'app/mobile-app'
  - 'portals/*'
  - 'packages/ui'
  - 'packages/shell'
  - 'packages/pod-form'
  - 'packages/media-picker'
  - 'packages/ad-request-form'
exports:
  - 'parseApiError'
  - 'isNetworkFailureMessage'
  - 'OFFLINE_MESSAGE'
  - 'GENERIC_ERROR_MESSAGE'
  - 'formatMoney'
  - 'formatINR'
  - 'base64ToBlob'
  - 'downloadBlob'
  - 'downloadBase64File'
  - 'downloadTextFile'
  - 'fileToBase64'
  - 'fileToDataUrl'
  - 'formatMjml'
  - 'nationalPhoneDigits'
  - 'isStoryLive'
  - 'videoSourceUrl'
  - 'podPhase'
  - 'canCompletePod'
  - 'splitPodsByPhase'
  - 'hostPodSection'
  - 'splitHostPods'
  - 'POD_LIVE_TAIL_MS'
  - 'clubCategoryKey'
  - 'productMatchesClub'
  - 'filterProductsForClub'
  - 'payableSpots'
  - 'payingAttendees'
  - 'HOST_FREE_SPOT_NOTE'
  - 'PodSpotLimits'
  - 'buildEarningsStatement'
  - 'formatStatementMoney'
  - 'GRIEVANCE_FIELDS'
  - 'GRIEVANCE_MAX_LENGTH'
  - 'GRIEVANCE_OPTIONAL_FIELDS'
  - 'GRIEVANCE_STATUSES'
  - 'EMPTY_GRIEVANCE_DRAFT'
  - 'isGrievanceFieldRequired'
  - 'isGrievanceOpen'
  - 'grievanceFieldLabelKey'
  - 'SUBMIT_GRIEVANCE_SDL'
  - 'GRIEVANCE_OFFICER_SDL'
  - 'podMediaPath'
  - 'podMediaLink'
  - 'mwebPodMediaLabels'
  - 'shellPodMediaLabels'
  - 'buildPodMediaLabels'
  - 'REFERRAL_PARAM'
  - 'referralLink'
  - 'readReferralCode'
  - 'renderReferralMessage'
  - 'DEFAULT_REFERRAL_MESSAGE'
  - 'REFERRAL_MESSAGE_TOKENS'
  - 'APP_POPUP_DISMISSED_KEY'
  - 'APP_POPUP_MAX_WIDTH'
  - 'APP_POPUP_HEIGHT_FRACTION'
  - 'APP_POPUP_VIEWPORT_GUTTER'
  - 'APP_POPUP_DEFAULT_ASPECT'
  - 'parseDismissedPopupIds'
  - 'withDismissedPopupId'
  - 'isPopupDismissed'
  - 'readDismissedPopupIds'
  - 'rememberDismissedPopup'
  - 'appPopupAspect'
  - 'appPopupLimits'
  - 'appPopupImageSize'
  - 'detectClientPlatform'
  - 'followActionFor'
  - 'nextFollowStatus'
  - 'readFollowStatus'
  - 'followStatusFrom'
  - 'FOLLOW_LABEL_KEY'
  - 'followButtonLabelKey'
  - 'followRequestRowState'
  - 'offersFollowBack'
  - 'followBackLabelKey'
  - 'canFollowBack'
  - 'followOutcomeLabelKey'
  - 'CONTACT_CHANNELS'
  - 'isPhoneChannel'
  - 'formatPhoneLine'
  - 'currentContactValue'
  - 'contactDraftFrom'
  - 'contactDraftValue'
  - 'contactDraftIsUnchanged'
  - 'emptyContactDraft'
  - 'buildContactChangeLabels'
  - 'USERNAME_PATTERN'
  - 'normalizeUsername'
  - 'usernameStatus'
  - 'canSaveUsername'
  - 'isUsernameError'
  - 'usernameBlocksSave'
  - 'usernameFieldState'
  - 'scheduleUsernameCheck'
  - 'IDLE_USERNAME_CHECK'
  - 'USERNAME_CHECK_DEBOUNCE_MS'
  - 'profileUrl'
  - 'buildUsernameLabels'
  - 'BADGE_CONDITIONS'
  - 'BADGE_GOAL_KEY'
  - 'BADGE_WINDOWS'
  - 'BADGE_WINDOW'
  - 'BADGE_WINDOW_KEY'
  - 'badgeProgressPercent'
  - 'sortBadgeProgress'
  - 'AUTO_POD_ROLES'
  - 'autoPodTicks'
  - 'autoPodEnrolledCount'
  - 'autoPodPreLive'
  - 'autoPodActionable'
  - 'splitAutoPods'
  - 'autoPodMissingRoles'
  - 'autoPodWaitingOn'
  - 'autoPodHostNeedsLocation'
  - 'autoPodCityLabel'
  - 'autoPodModeCount'
  - 'mwebAutoPodLabels'
  - 'shellAutoPodLabels'
  - 'shellPodKindLabels'
  - 'DRAFT_EXPIRY_WARNING_MS'
  - 'draftMsLeft'
  - 'draftHoursLeft'
  - 'isDraftExpiringSoon'
  - 'splitDraftsByExpiry'
  - 'PodAttendanceMode'
  - 'attendanceRowState'
  - 'splitAttendance'
  - 'needsOtp'
  - 'canScanTickets'
  - 'earningsBodyFor'
  - 'mwebAttendanceLabels'
  - 'shellAttendanceLabels'
  - 'buildAttendanceLabels'
---

## Why this exists

Each of these modules replaced a byte-for-byte copy that had been pasted into every surface.
`parse-api-error.ts` existed once in mWeb and once in all sixteen portals.
(`duid.ts` lived here too, until the native app started sharing the id — it moved to
`@duncit/user-core`, which the app can consume without a transitive `@duncit/*` dependency.)
The base64 → Blob → anchor-click download flow was rewritten in admin, finance, crm (twice),
support and legal. `fileToDataUrl` had eight implementations, one of them inside
`@duncit/media-picker`. Money formatting had a `formatMoney`, a `money(symbol, v)` and a `fmt`
in partners-app, finance, products and crm that all rounded differently.

The domain modules are here for a stronger reason: a wrong copy is a wrong number.
`payableSpots` encodes "the host sits in `pod_attendees` and never pays", and
`buildEarningsStatement` is the single builder mWeb and the native app both render at
Create-a-Pod Step 4 — rule 27 says those two screens must be identical, and sharing the builder
is how that is enforced rather than hoped for.

Zero runtime dependencies, on purpose. `parse-api-error.ts` types the Apollo error
**structurally**, so this package never has to depend on `@apollo/client`.

## Use it

From `app/mweb/src/apollo.ts` and `portals/crm/src/components/ExcelImportDialog.tsx`:

```ts
import { fileToBase64, parseApiError, formatMoney } from '@duncit/utils';


try {
  const base64 = await fileToBase64(file);
  await importLeads({ variables: { file: base64 } });
} catch (err) {
  setError(parseApiError(err));
}

formatMoney(125000);                                   // "₹1,25,000"
formatMoney(1250, { symbol, decimals: 2, grouping: false }); // "₹1250.00"
formatMoney(125000, { compact: true });                // "₹1.3L" — rounds to 1 decimal
```

And the Step-4 statement, identically in `app/mweb/.../price-panel/index.tsx` and
`app/mobile-app/src/components/create-pod/price-panel/index.tsx`:

```ts
import { buildEarningsStatement, formatStatementMoney } from '@duncit/utils';

const statement = buildEarningsStatement(waterfall, { has_venue: !!slotId, symbol });
```

## Sample data

One fixture per family, using the real field names. Copy it into a scratch file and the
worked examples below reproduce exactly.

```ts
/** The `potentialPodEarnings` waterfall, verbatim from the server (rupees). */
const waterfall = {
  amount: 29000,                  // 29 payable spots × ₹1,000
  gst_pct: 18,
  gst_amount: 4423.73,
  net_amount: 24576.27,
  platform_fee_pct: 10,
  platform_fee_amount: 2457.63,
  pool_amount: 22118.64,
  club_admin_pct: 5,
  club_admin_amount: 1105.93,
  venue_amount: 6000,
  venue_commission_pct: 10,
  venue_commission_amount: 600,
  venue_receives: 5400,
  host_amount: 15012.71,
  host_commission_pct: 12,
  host_commission_amount: 1801.53,
  host_receives: 13211.18,
  host_earn_pct: 45.56,
};

/** A club and the product-picker options offered against it. */
const club = {
  _id: 'clb_7742',
  name: 'Bandra Badminton Club',
  super_category_id: 'sc_sports',
  category_id: 'sub_badminton',   // a club's Sub lives in category_id
};

const products = [
  { _id: 'prd_101', name: 'Yonex Astrox 88D Pro',
    categories: [{ super_category_id: 'sc_sports', sub_category_id: 'sub_badminton' }] },
  { _id: 'prd_102', name: 'Cosco Football Size 5',
    categories: [{ super_category_id: 'sc_sports', sub_category_id: 'sub_football' }] },
  { _id: 'prd_103', name: 'Duncit Cotton Tee' },   // no category data at all
];

/** The two Apollo error shapes `parseApiError` is fed in production. */
const offlineError = { message: 'Failed to fetch', networkError: { message: 'Failed to fetch' }, graphQLErrors: [] };
const rejectedError = { message: 'GraphQL error', graphQLErrors: [{ message: 'Pod DUN-POD-8842 is already full' }] };
```

## Worked examples

Every output below was produced by running the real export against the fixtures above.

### Money

```ts
formatMoney(125000)                                    // '₹1,25,000'
formatMoney(1250, { decimals: 2, grouping: false })    // '₹1250.00'   (finance `money(symbol, v)`)
formatMoney(125000, { compact: true })                 // '₹1.3L'
formatMoney(2450000, { compact: true })                // '₹24.5L'
formatMoney(99999,  { compact: true })                 // '₹99,999'    below the 1-lakh threshold
formatMoney(1500, { symbol: '$', decimals: 2 })        // '$1,500.00'

formatINR(125000)                                      // '₹1,25,000'
formatINR(0)                                           // '₹0'
formatStatementMoney(13211.18, '₹')                    // '₹13,211.18'
```

`compact` only engages at an absolute value of ₹1,00,000 or more — `99999` still groups in
full. That threshold is why a "compact" dashboard tile and a detail row can legitimately show
the same number two ways.

### Pod spot economics

```ts
[30, 1, 0, 12.7].map(payableSpots)                     // [29, 0, 0, 11]

payingAttendees(
  ['usr_9f2', 'usr_a13', 'usr_b77', 'usr_c04'],        // pod_attendees
  ['usr_9f2'],                                         // hosts
)                                                      // 3

HOST_FREE_SPOT_NOTE
// 'Your spot is free — the calculation is based on total spots − 1.'
```

A 30-spot pod bills 29. `1` (host only) and `0` (unlimited/unset) both bill nothing rather
than going negative, and a fractional count floors before subtracting.

### API errors

```ts
parseApiError(offlineError)
// 'Unable to connect to server. Please check your internet connection and try again.'

parseApiError(rejectedError)
// 'Pod DUN-POD-8842 is already full'

parseApiError(null)
// 'Something went wrong. Please try again.'

parseApiError({}, 'Could not load your pods.')
// 'Could not load your pods.'
```

### Cart entry point

```ts
deriveCartEntry(3, '/shop')
// { visible: true,  count: 3,   badge: '3',   label: 'Open cart (3 items)' }

deriveCartEntry(0, '/shop')
// { visible: false, count: 0,   badge: '0',   label: 'Open cart (0 items)' }

deriveCartEntry(120, '/cart')
// { visible: false, count: 120, badge: '99+', label: 'Open cart (120 items)' }

cartBadgeLabel(99)   // '99'
cartBadgeLabel(100)  // '99+'

isCartFlowRoute('/shop')                  // false
isCartFlowRoute('/cart')                  // true
isCartFlowRoute('/product-checkout/pod-1')// true
isCartFlowRoute('ProductCheckout')        // true  — native screen name, same answer
isCartFlowRoute('/pod/DUN-POD-8842')      // false
```

The web path and the native screen name reduce to the same key, which is what keeps the
header identical on both surfaces (rule 27).

### Club ↔ product matching

```ts
clubCategoryKey(club)
// { superId: 'sc_sports', subId: 'sub_badminton' }

clubCategoryKey({ _id: 'clb_0001', name: 'Legacy Club' })
// null — no Super+Sub, so this pod has no category at all

filterProductsForClub(products, club).map((p) => p._id)
// ['prd_101']

productMatchesClub(products[1], clubCategoryKey(club))   // false — football, not badminton

pruneProductRequests(values.product_requests, filterProductsForClub(products, club))
// drops rows whose product the newly-picked club no longer offers
```

Both helpers **fail closed**. A null key means "this pod has no category", so nothing matches
and the picker renders `No products available for this category.` — it does *not* fall back to
the whole catalogue. `prd_103` is dropped for the same reason: it carries no category data.

That mirrors the server gate in `pod.service`, which rejects exactly this set on submit, so the
picker can never offer something the save would refuse.

`pruneProductRequests` returns the **same array reference** when nothing is dropped, which lets
a caller use it directly as an effect guard without looping.

### The Step-4 earnings statement

```ts
const statement = buildEarningsStatement(waterfall, { has_venue: true, symbol: '₹' });
```

Returns, exactly:

```json
{
  "collection": {
    "label": "Total collection",
    "amount": 29000,
    "included_gst_note": "Includes GST ₹4,423.73 — prices are GST-inclusive"
  },
  "sections": [
    {
      "key": "taxes",
      "title": "Taxes",
      "lines": [
        { "key": "taxable", "label": "Taxable Amount", "amount": 24576.27,
          "formula": "₹29,000.00 − ₹4,423.73 GST (prices are GST-inclusive)", "deduction": false },
        { "key": "gst", "label": "GST @18%", "amount": 4423.73,
          "formula": "₹24,576.27 × 18%", "deduction": true }
      ],
      "total": 4423.73
    },
    {
      "key": "platform",
      "title": "Platform Charges",
      "lines": [
        { "key": "platform-fee", "label": "Platform Fee @10%", "amount": 2457.63,
          "formula": "₹24,576.27 × 10%", "deduction": true },
        { "key": "duncit-commission", "label": "Duncit Commission @12%", "amount": 1801.53,
          "formula": "₹15,012.71 × 12% (your remainder)", "deduction": true }
      ],
      "total": 4259.16
    },
    {
      "key": "club",
      "title": "Club Charges",
      "lines": [
        { "key": "club-admin", "label": "Club Admin Fee @5%", "amount": 1105.93,
          "formula": "₹22,118.64 × 5%", "deduction": true }
      ],
      "total": 1105.93
    },
    {
      "key": "venue",
      "title": "Venue Charges",
      "lines": [
        { "key": "venue-slot", "label": "Venue Slot Price", "amount": 6000,
          "formula": "Fixed booked slot price (deducted once per pod)", "deduction": true },
        { "key": "venue-commission", "label": "Duncit Commission from Venue @10%", "amount": 600,
          "formula": "₹6,000.00 × 10% of the slot price above — the venue receives ₹5,400.00", "deduction": false }
      ],
      "total": 6000
    }
  ],
  "total_deductions": 15788.82,
  "net_payout": { "collection": 29000, "total_deductions": 15788.82, "receives": 13211.18 },
  "reconciled": true
}
```

Three things to read off that output:

- The `taxable` line has `deduction: false` — it is context, and the Taxes section total
  (`4423.73`) is the GST alone. A renderer that sums every line will over-state the section.
- So does `venue-commission`, for a different reason. The two Duncit commissions are charged
  on different bases: `duncit-commission` (**Platform Charges**) is `host_amount ×
  host_commission_pct` and really does come out of the collection, venue or no venue;
  `venue-commission` is `venue_amount × venue_commission_pct`, already inside the ₹6,000 slot
  price on the row above it. So Venue Charges totals `6000`, not `6600` — the row is there to
  say what the venue is left with, never to be deducted twice.
- `has_venue: false` only drops the Venue section; nothing moves between sections. With
  `venue_amount: 0`, `host_amount: 21012.71`, `host_commission_amount: 2521.53` and
  `host_receives: 18491.18` the platform total becomes `4979.16` and `total_deductions`
  becomes `10508.82`.
- `reconciled: true` means all three audit identities held within ±₹0.02. If it comes back
  `false`, the server waterfall is inconsistent — do not "fix" it here.

## API

### Money

| Export | Signature | Default | Meaning |
| --- | --- | --- | --- |
| `formatINR` | `(value: number) => string` | — | Whole-rupee INR via `Intl`, `en-IN` grouping. |
| `formatMoney` | `(value: number, options?: FormatMoneyOptions) => string` | `{}` | Superset formatter covering every portal variant. |
| `FormatMoneyOptions.symbol` | `string` | `'₹'` | Currency prefix; pass a server-provided symbol. |
| `FormatMoneyOptions.decimals` | `number` | `0` | Fixed fraction digits. |
| `FormatMoneyOptions.grouping` | `boolean` | `true` | `false` reproduces the plain `toFixed` family. |
| `FormatMoneyOptions.compact` | `boolean` | `false` | `₹1.2L` notation at \|value\| ≥ 1,00,000. |
| `formatStatementMoney` | `(value: number, symbol: string) => string` | — | The one `₹X,XXX.XX` format both Step-4 surfaces render. |

### AI content check — `ai-monitor`

| Export | Signature | Meaning |
| --- | --- | --- |
| `AI_MONITOR_GRADIENT` | `readonly [string, string, string]` | Violet → pink → amber stops; pass straight to `expo-linear-gradient`. |
| `AI_MONITOR_GRADIENT_CSS` | `string` | The same stops as a `linear-gradient(…)` value for MUI `sx`. |

Four surfaces paint it — the "AI monitoring" chip and the Create Pod waiting overlay, on mWeb
and on native. They have to read as the same feature, so the stops live here rather than as
four literals that drift apart.

### Checkout bill — `checkout-bill`

| Export | Signature | Meaning |
| --- | --- | --- |
| `round2` | `(n: number) => number` | The finance engine's single-round rule; non-numbers become `0`. |
| `clampPayable` | `(amount: number) => number` | What is left to charge: rounded, never below zero. |
| `applyBillDiscounts` | `(gross: number, discounts: BillDiscount[]) => DiscountedBill` | Takes the deductions off in order, stopping at zero. |
| `maxRedeemableCoins` | `(balance: number, payableAfterCoupon: number) => number` | Whole coins the bill can absorb — balance, bill and gateway minimum. |
| `MIN_GATEWAY_CHARGE` | `1` | Smallest amount Razorpay accepts; server twin of the same constant. |

A coupon and Duncit Coins cut the same gross and neither may cut past zero. `applyBillDiscounts`
caps each line at what was still owed when its turn came, so the printed rows always reconcile
to `payable` and the leftover discount is simply dropped — never refunded, never carried:

```ts
applyBillDiscounts(399, [{ key: 'coupon', label: 'WELCOME', amount: 500 }])
// { discounts: [{ key: 'coupon', label: 'WELCOME', amount: 399 }], discountTotal: 399, payable: 0 }

maxRedeemableCoins(500, 359.1)      // 358 — 359 would leave ₹0.10, under the ₹1 gateway minimum
clampPayable(359.1 - 358)           // 1.1  — not 1.0999999999999773
```

### Pod economics

| Export | Signature | Meaning |
| --- | --- | --- |
| `payableSpots` | `(totalSpots: number) => number` | `total − 1`; never negative. |
| `payingAttendees` | `(attendeeIds, hostIds) => number` | Attendees minus hosts; both args accept `null`/`undefined`. |
| `attendeeSeatCount` | `(people: { seats?: number }[]) => number` | Seats a rendered attendee list holds — a booking for three is one row, so counting rows under-reports it. `seats` absent or `0` counts as one. |
| `HOST_FREE_SPOT_NOTE` | `string` | Copy to render beside any spots-driven figure. |
| `PodSpotLimits` | `type` | The range a pod that already EXISTS may be resized within, as the server's `podSpotLimits` answers it: `current`, `min`, `max`, `seats_taken`, `venue_capacity`, `min_pax`, `slidable`, `can_decrease`. |

`spotsBounds` is the CREATE-time answer, derived on the client from the slot being picked.
`PodSpotLimits` is the EDIT-time one, and only the server can produce it: a live pod's slot is
BOOKED, so `venueAvailableSlots` no longer returns it, and the seats already sold — which set
the floor — were never on the client at all. The type lives here so mWeb, the portals' pod form
and the native app read one definition; each still writes its own literal GraphQL document,
because native's codegen refuses an interpolated one.
| `buildEarningsStatement` | `(w: EarningsWaterfall, o: EarningsStatementOptions) => EarningsStatement` | Formats the server waterfall; adds no math. |
| `EarningsStatementOptions.has_venue` | `boolean` (required) | Renders the Venue Charges section. |
| `EarningsStatementOptions.symbol` | `string` (required) | Symbol used inside the formula strings. |

### Errors

| Export | Signature | Default | Meaning |
| --- | --- | --- | --- |
| `parseApiError` | `(err: unknown, fallback?: string) => string` | `GENERIC_ERROR_MESSAGE` | Apollo/network error → user-facing message. |
| `isNetworkFailureMessage` | `(message: string) => boolean` | — | True for "Failed to fetch" / "Network request failed" / "Load failed". |
| `OFFLINE_MESSAGE` | `string` | — | Connectivity copy. |
| `GENERIC_ERROR_MESSAGE` | `string` | — | Catch-all copy. |

### Cart, files, categories

| Export | Signature | Default | Meaning |
| --- | --- | --- | --- |
| `deriveCartEntry` | `(count: number, route: string) => CartEntry` | — | `{ visible, count, badge, label }` for the header entry point. |
| `cartBadgeLabel` | `(count: number) => string` | — | Capped badge text. |
| `isCartFlowRoute` | `(route: string) => boolean` | — | True on cart / checkout / product-checkout, web path or native name. |
| `CART_BADGE_MAX` | `99` | — | Cap before `99+`. |
| `MEETING_PLATFORM_VALUES` | `['GOOGLE_MEET','ZOOM','TEAMS','OTHER']` | — | The codes stored in `Pod.meeting_platform`. |
| `meetingPlatformOptions` | `(otherLabel: string) => {value,label}[]` | — | The Create-a-Pod dropdown. Only `OTHER` is copy; the rest are product names, identical in every language. |
| `meetingPlatformName` | `(value: string) => string` | — | `GOOGLE_MEET` → `Google Meet`. Unknown values title-cased, never raw. Also decodes read-only legacy codes (`MICROSOFT_TEAMS`, `SKYPE`, `WEBEX`). |
| `isMeetingPlatform` | `(value: unknown) => boolean` | — | Whether a stored value is one this build offers. Used by both create-pod zod schemas. |
| `fileToDataUrl` | `(file: Blob) => Promise<string>` | — | Full `data:<mime>;base64,…` URL. |
| `fileToBase64` | `(file: Blob) => Promise<string>` | — | Bare base64, prefix stripped. |
| `base64ToBlob` / `downloadBlob` / `downloadBase64File` / `downloadTextFile` | see source | — | The browser download flow, once. |
| `clubCategoryKey` | `(club) => ClubCategoryKey \| null` | — | Super+Sub pair, `null` for legacy clubs. |
| `productMatchesClub` | `(product, key) => boolean` | — | Fails closed: `false` when `key` is `null` or the product has no category data. |
| `filterProductsForClub` | `(products, club) => products` | — | The product-picker filter. |
| `formatMjml` | `(mjml: string) => string` | — | Pretty-prints an MJML template. |
| `nationalPhoneDigits` | `(raw, length = 10) => string` | — | Display-only local digits: "+91 9876543210" → "9876543210". |
| `isStoryLive` | `(expiresAt, now = Date.now()) => boolean` | — | Client half of the 24h story expiry; `null` = permanent post. |
| `videoSourceUrl` | `(url) => string` | — | The URL to hand a video player: our ImageKit addresses ask for the stored file, everything else is unchanged. |

### Pod phase — the Home rails

A pod is not simply "upcoming or past". It leaves the upcoming feed the moment
it STARTS — nobody can join a pod that is running, and the server refuses the
booking — but it is not *previous* until it is actually over. Between those two
instants it is **ONGOING**, and Home gives it its own rail on both mWeb and
native. Before this split a pod that was happening right now dropped straight
into Previous Pods.

A pod's end is `pod_end_date_time` when the host set one, and
`pod_date_time + POD_LIVE_TAIL_MS` (4h) when they did not — the same fallback
the server states as a Mongo predicate in `pod.lifecycle.ts`, which is why the
tail is one constant rather than a number typed out per surface. mWeb's
`podStatus` and native's `podStatus` both read this, so a status chip and the
rails can never place the same pod differently.

| Export | Signature | Meaning |
| --- | --- | --- |
| `POD_LIVE_TAIL_MS` | `number` | 4h — how long a pod with no set end stays live. |
| `podPhase` | `(start?, end?, now = Date.now()) => PodPhase` | `UPCOMING` before the start, `ONGOING` through the end instant, `PREVIOUS` after. Unusable start *or* end ⇒ never "previous" on a guess. |
| `splitPodsByPhase` | `(pods, now = Date.now()) => { upcoming, ongoing, previous }` | One pass, one clock read, so the three rails cannot disagree at a boundary. |
| `canCompletePod` | `(pod, now = Date.now()) => boolean` | True only on a PREVIOUS pod. Completion settles the payout off the seats scanned in, so Host Studio offers it once the door has shut — never on an upcoming or ongoing pod. |

### Draft-pod expiry

A Create-Pod draft is deleted `draft_retention_days` (Admin > Pods > Pod Settings,
default 3) after it was **created** — not after its last save, so an autosave cannot
keep an abandoned draft alive forever. The server stamps the date on
`PodDraft.expires_at` from the same setting its retention sweep deletes on, so the
countdown a host reads can never promise a day the sweep disagrees with.

Host Studio lifts every draft inside the last 24 hours out of the list into an
info-badge panel at the top, highlighted, soonest-deleted first. mWeb and the native
app share these derivations and keep their own MUI / Tamagui rows (rules 27 + 40).

| Export | Signature | Meaning |
| --- | --- | --- |
| `DRAFT_EXPIRY_WARNING_MS` | `number` | 24h — the window that promotes and highlights a draft. |
| `draftMsLeft` | `(draft, now = Date.now()) => number | null` | Milliseconds to deletion. `null` with no usable `expires_at`; negative once past due (the sweep runs daily). |
| `isDraftExpiringSoon` | `(draft, now = Date.now()) => boolean` | Inside the warning window — the one rule behind both the promotion and the highlight. |
| `draftHoursLeft` | `(draft, now = Date.now()) => number` | Whole hours left, floored at 0. `0` means the last hour, not "already gone". |
| `splitDraftsByExpiry` | `(drafts, now = Date.now()) => { expiring, rest }` | `expiring` soonest-first; `rest` keeps the server order. |

### Host Studio sections

A hosted pod's VENUE-APPROVAL state, not its clock, decides which of Host
Studio’s three lists it belongs to — Requested Pods above Your Pods, Rejected
Pods below it — and a pod is only ever in one of them. The split is recomputed
from `venue_approval_status` on every read, so the venue’s decision landing is
the whole mechanism: nothing has to remember to take a pod out of one list when
it puts it in another. mWeb and the native app both render from this; only the
MUI and Tamagui views differ (rules 27 + 40).

| Export | Signature | Meaning |
| --- | --- | --- |
| `hostPodSection` | `(status?) => HostPodSection` | `PENDING` ⇒ `REQUESTED`, `DECLINED` ⇒ `REJECTED`. `NONE`, `APPROVED` and anything unrecognised answer `YOURS` — a pod whose state cannot be read is still a pod the host runs. |
| `splitHostPods` | `(pods) => { requested, yours, rejected }` | One pass, order preserved within each list. `rejected` empty is what hides the Rejected Pods section entirely. |

### Notifications

The server stores no notification "type", so the category is derived from the title.
mWeb and native each keep only their own icon map (MUI vs MaterialIcons); the
classification and the chip filter live here so the twins cannot drift.

| Export | Signature | Meaning |
| --- | --- | --- |
| `notificationCategory` | `(title) => NotificationCategory` | First matching keyword rule wins; unmatched → `'general'`. |
| `notificationChips` | `(titles) => NotificationChip[]` | `All` first, then only the categories present, each with its count. |
| `matchesNotificationFilter` | `(title, key) => boolean` | `key === 'all'` or the title's category equals it. |
| `NOTIFICATION_CATEGORY_ORDER` | `readonly NotificationCategory[]` | Chip order — rule order, catch-all last. |
| `NOTIFICATION_CATEGORY_LABEL` | `Record<NotificationCategory, string>` | Chip copy per category. |

```ts
notificationCategory('Payment received')            // 'payment'
notificationCategory('Your pod starts soon')        // 'pod'
notificationCategory('')                            // 'general'

notificationChips(['Payment received', 'Pod starts soon', 'Pod full'])
// [{ key: 'all', label: 'All', count: 3 },
//  { key: 'payment', label: 'Payments', count: 1 },
//  { key: 'pod', label: 'Pods', count: 2 }]

matchesNotificationFilter('Payment received', 'all')     // true
matchesNotificationFilter('Payment received', 'pod')     // false
```

### Pod feedback — `pod-feedback`

Rating a pod is not one question. `POD_FEEDBACK_ASPECTS` is the order the parts are
asked in; which parts a pod HAS is the server's answer (`feedback_aspects` on the
pending pod), so neither app carries that rule.

| Export | Signature | Meaning |
| --- | --- | --- |
| `POD_FEEDBACK_ASPECTS` | `readonly PodFeedbackAspect[]` | Asking order — `OVERALL` first, `OTHER` last. |
| `POD_FEEDBACK_ASPECT_KEY` | `Record<PodFeedbackAspect, string>` | i18n key per aspect (apps — rule 38). |
| `POD_FEEDBACK_ASPECT_LABEL` | `Record<PodFeedbackAspect, string>` | Plain English per aspect (portals). |
| `orderedAspects` | `(fromServer) => PodFeedbackAspect[]` | Server's list, deduped, ordered, `OVERALL` guaranteed. |
| `canSubmitPodFeedback` | `(scores) => boolean` | Only `OVERALL` is required. |
| `buildPodFeedbackInput` | `({podId, scores, message, aspects}) => PodFeedbackInput` | The mutation payload. |
| `POD_FEEDBACK_REMINDER_OPTIONS` | `readonly {choice: PodFeedbackReminderChoice; labelKey: string}[]` | The two ways out of the prompt, in the order they are offered. |

```ts
orderedAspects(['FOOD', 'HOST', 'NOT_A_THING'])
// ['OVERALL', 'HOST', 'FOOD']

canSubmitPodFeedback({ HOST: 5 })                    // false — no overall score
canSubmitPodFeedback({ OVERALL: 4 })                 // true

buildPodFeedbackInput({
  podId: 'p1',
  scores: { OVERALL: 4, HOST: 5, FOOD: 0 },
  message: '  ',
  aspects: ['OVERALL', 'HOST', 'FOOD'],
})
// { pod_id: 'p1', rating: 4, message: null, ratings: [{ aspect: 'HOST', rating: 5 }] }
// FOOD is absent, not zero: "not scored" and "scored badly" are different answers.
```

Closing the prompt without answering it is an answer too, and the server keeps it —
otherwise the dismiss lives in memory and the same pod is back on the next page load.
`POD_FEEDBACK_REMINDER_OPTIONS` is the pair of ways out, so mWeb and native offer the
same two in the same order:

```ts
POD_FEEDBACK_REMINDER_OPTIONS
// [{ choice: 'LATER', labelKey: 'mweb.podFeedback.remindLater' },
//  { choice: 'NEVER', labelKey: 'mweb.podFeedback.remindNever' }]
```

`LATER` snoozes THIS pod for a day (`remindPodFeedback(pod_id, LATER)`); `NEVER` retires
it. Neither touches the other pods the guest was marked present at.

The category is deliberately NOT sent — the server derives it from the weakest score,
so a guest is never asked to triage their own feedback.

### Pod attendance — `pod-attendance` + `pod-attendance-copy`

Attendance is what a host is paid on, so every rule the roster renders by lives here and
the MUI page (`@duncit/host-pod-actions`) and the native Tamagui screen both read it
(rule 27). The board is `podAttendanceBoard(pod_doc_id)` — the ONLY read of a roster — and
it carries `pod_mode`, because a virtual pod has no door: nobody scans a ticket there. A
joined member is marked present the moment they open the meeting link from the pod page
inside the pod window (`joinPodMeeting`, recorded as method `VIRTUAL_JOIN`).

| Export | Signature | Meaning |
| --- | --- | --- |
| `PodAttendanceMode` | `'PHYSICAL' \| 'VIRTUAL'` | Mirrors the server's `PodMode`; the board's `pod_mode`. |
| `attendanceRowState` | `(row, canMark) => AttendanceRowState` | `MARKED`, `READY`, `NEEDS_COMPANIONS` or `LOCKED` — one value, never three booleans. |
| `splitAttendance` | `(rows) => { marked, unmarked }` | The two lists, in the order the page shows them. |
| `needsOtp` | `(board) => boolean` | HOST viewer AND `otp_required`. The Club Admin's override never asks — it exists for the attendee who cannot be reached. |
| `canScanTickets` | `(board) => boolean` | HOST viewer AND `can_mark` AND not `VIRTUAL`. False hides the scan CTA. |
| `earningsBodyFor` | `(board, labels) => string` | `labels.earningsBodyVirtual` for a virtual pod, `labels.earningsBody` otherwise — the sentence above the roster. |
| `mwebAttendanceLabels` / `shellAttendanceLabels` / `buildAttendanceLabels` | `(t) => PodAttendanceLabels` | Every word the surfaces render — `mweb.attendance.*` for mWeb + native, `shell.attendance.*` for the portals, word-for-word identical. |

```ts
const labels = mwebAttendanceLabels(t);

// The head of a podAttendanceBoard answer; rows and counts trimmed.
const board = {
  pod_id: 'DUN-POD-4821',
  viewer: 'HOST',
  can_mark: true,
  otp_required: true,
  pod_mode: 'PHYSICAL',
} as const;

needsOtp(board)                   // true  — prove the number before a by-hand mark
canScanTickets(board)             // true  — there is a door to scan at
earningsBodyFor(board, labels)    // 'Your earnings are calculated only from the attendees you mark. …'

const online = { ...board, pod_id: 'DUN-POD-5502', pod_mode: 'VIRTUAL' } as const;
canScanTickets(online)            // false — the scan CTA is hidden
earningsBodyFor(online, labels)   // 'This pod is online, so there is no door to scan at. …'
labels.methodLabel('VIRTUAL_JOIN')  // 'Joined the meeting' — what the row reads after joinPodMeeting

needsOtp({ viewer: 'CLUB_ADMIN', otp_required: true })  // false — the override never asks
```

A virtual host can still mark someone by hand (`HOST_MANUAL`, with a code when the
setting says so) for the member who joined some other way; only the scanner is gone.

### Auto Pods — `auto-pod` + `auto-pod-copy`

An Auto Pod is a pod written without a venue, a host or a club; a venue, a host and a club
admin each enrol later, in ANY order, and it becomes a real pod once all three have. The row
shape, the three-enrolment derivation every card draws, the "can this role act" predicate and
the city-pin rule all live here, so the MUI card (`@duncit/auto-pods`) and the native Tamagui
twin cannot disagree on what an offer is waiting for.

| Export | Signature | Meaning |
| --- | --- | --- |
| `AUTO_POD_ROLES` | `readonly AutoPodRole[]` | `['venue', 'host', 'club']` — the order every tick row and "waiting for" line lists them. Presentation only. |
| `autoPodTicks` | `(row) => AutoPodTick[]` | Always three, in that order, `done` when that partner has enrolled. |
| `autoPodEnrolledCount` | `(row) => number` | How many of the three have enrolled. |
| `autoPodPreLive` | `(stage) => boolean` | `OPEN` (nobody yet) or `CLAIMING` (at least one) — still enrolling. |
| `autoPodActionable` | `(row, role) => boolean` | Pre-live, this viewer has not enrolled, and this role's tick is empty. Any order. |
| `splitAutoPods` | `(rows, role) => { actionable, mine }` | A role's queue: what needs them, then what they already took. |
| `autoPodMissingRoles` | `(row) => AutoPodRole[]` | Every role still missing, in tick order; `[]` once live or gone. |
| `autoPodWaitingOn` | `(row) => AutoPodRole \| null` | The first missing role. |
| `autoPodHostNeedsLocation` | `(row, locationId) => boolean` | True when the offer is unpinned AND the host has no city selected — their "Assign Myself" must wait. |
| `autoPodCityLabel` | `(location) => string` | `"Bengaluru, Karnataka"`; `''` for an unpinned offer. |
| `autoPodModeCount` | `(counts, mode) => number` | How many offers wait on a studio mode (`HOST` / `VENUE` / `CLUB`); 0 for any other. |
| `mwebAutoPodLabels` / `shellAutoPodLabels` | `(t) => AutoPodLabels` | Every word the queue renders — `mweb.autoPods.*` for mWeb + native, `shell.autoPods.*` for the portals, word-for-word identical. |
| `shellPodKindLabels` | `(t) => PodKindLabels` | The "Normal Pod or Auto Pod?" question a portal's New Pod button asks. |

```ts
// A club admin enrolled first: the offer is CLAIMING, pinned to their club's city.
const row: AutoPodRow = {
  id: 'apod-000123',
  auto_pod_no: 'APOD-000123',
  stage: 'CLAIMING',
  pod_title: 'Sunday Badminton Doubles',
  pod_amount: 499,
  no_of_spots: 8,
  venue_claim: null,
  host_claim: null,
  club_claim: { club_id: 'c-41', club_name: 'Koramangala Smashers', user_id: 'u-9', claimed_at: '…' },
  location: { location_id: 'loc-blr', location_name: 'Bengaluru', country: 'India', state: 'Karnataka', city: 'Bengaluru', bound_by: 'CLUB', bound_at: '…' },
  viewer_claimed: false,
  // …
};

autoPodMissingRoles(row)                 // ['venue', 'host']
autoPodActionable(row, 'venue')          // true  — a venue in Bengaluru may accept
autoPodActionable(row, 'club')           // false — that tick is taken
autoPodHostNeedsLocation(row, '')        // false — pinned; the host's selection is not consulted
autoPodHostNeedsLocation({ location: null }, '')        // true  — unpinned and no city selected
autoPodCityLabel(row.location)           // 'Bengaluru, Karnataka'
mwebAutoPodLabels(t).waitingFor(autoPodMissingRoles(row))  // 'Waiting for a venue, a host'
```

## The grievance form is one spec, four validators

The same grievance is raised from mWeb, the native app and the main website, and gated
again by the server. Four validators means four chances to disagree about whether a field
is required or how long it may be — so the numbers live here once and each surface builds
its own schema from them:

```ts
GRIEVANCE_MAX_LENGTH.subject;          // 200 — the same number the server enforces
GRIEVANCE_OPTIONAL_FIELDS;             // ['address'] — the only field you may leave blank
isGrievanceFieldRequired('address');   // false
isGrievanceFieldRequired('phone');     // true
grievanceFieldLabelKey('subject');     // 'grievance.field.subject'
GRIEVANCE_FIELDS;                      // render order, shared so the two apps match
```

The value SHAPES are deliberately NOT here — email and phone patterns belong to
`@duncit/regex`, which already owns them. This module owns lengths, order and
optionality; nothing else.

`SUBMIT_GRIEVANCE_SDL` and `GRIEVANCE_OFFICER_SDL` are the operations as plain strings,
because the three surfaces wrap documents differently: Apollo's `gql` on mWeb,
`graphql-tag` in native, a bare `fetch` on the Astro site. A string is the one form all
three accept.

```ts
isGrievanceOpen('RECEIVED');   // true  — still owed an answer
isGrievanceOpen('RESOLVED');   // false
```

## No live preview

Framework-free by contract — there is no component to mount, so the page shows real
return values instead. The `<Preview>` runtime in `docs-site` is for MUI packages
(`@duncit/ui`, `@duncit/dialogs`, `@duncit/theme`).

## `fileToBase64` vs `fileToDataUrl` — they are not interchangeable

Two behavioural families exist because two families of server calls exist:

- `fileToDataUrl` resolves the **full** `data:<mime>;base64,…` URL (support, website-app,
  mWeb, media-picker payloads).
- `fileToBase64` resolves the **bare** base64 with the prefix stripped (crm, finance payloads).

Pick the one the endpoint you are calling already received. Swapping them ships a payload the
server cannot decode, and nothing type-checks the difference — both are `Promise<string>`.

## The host's spot is free — never bill the raw capacity

`payableSpots(total)` is `total − 1`, and returns `0` for `0` (unlimited/unset) and `1`
(host-only) rather than going negative. Every money figure derived from a spot count must go
through it: a 30-spot pod can only ever sell 29 seats. The server mirrors this in
`server/src/modules/finance/finance/breakdown.math.ts`; if the two ever disagree, Create-a-Pod,
the finance calculator and the real settlement disagree by exactly one ticket.

`payingAttendees(attendeeIds, hostIds)` is the head-count equivalent — hosts are written into
`pod_attendees` at creation, so counting the array raw over-states income by one ticket per host.

`HOST_FREE_SPOT_NOTE` is the copy that must sit next to any spots-driven figure.

## The earnings statement adds no math

`buildEarningsStatement` takes the `potentialPodEarnings` waterfall **verbatim**. It sums the
server's own lines for section subtotals, formats the formula strings, and sets `reconciled`
when all three audit identities hold. It does not compute a fee, a rate or a tax anywhere.

If a number looks wrong, the fix belongs in the server waterfall, not here — re-deriving it
client-side is how the two surfaces started disagreeing in the first place. GST is charged once
and extracted from the GST-inclusive collection, so the statement shows the real taxable base
rather than inventing per-charge tax rows.

Grouping is the one thing it does decide, and the commissions are where that matters. Duncit
takes a cut from each side of the split — `host_commission_*` off the host's remainder and
`venue_commission_*` off the venue's slot price — and only the first is money leaving the
host's collection. Reading one label off the other's amount is exactly the bug this grouping
now prevents.

## The referral link and its message

`referralLink(code, baseUrl)` and `readReferralCode(search)` are two halves of one agreement:
mWeb builds the link, the native app builds the same link, and mWeb's signup reads the code
back out of it. The parameter name lives here once (`REFERRAL_PARAM`) so none of the three can
drift.

`renderReferralMessage(template, { code, link, coins })` fills in what Finance wrote at
**Finance → Referrals**. The template is stored on the server with its placeholders intact —
`{code}`, `{link}`, `{coins}`, listed as `REFERRAL_MESSAGE_TOKENS` — and rendered per member at
share time, so one edit in Finance changes every share sheet at once. A blank template falls
back to `DEFAULT_REFERRAL_MESSAGE`; the coin count is never hardcoded in either, because a
message that promises fifty while the rate says ten is worse than no message.

```ts
renderReferralMessage('', { code: 'DUN-9F3A2C', link, coins: 50 })
// 'Join me on Duncit! We both earn 50 Duncit Coins. Use my code DUN-9F3A2C or sign up here: …'
```

## Every share is a tracked link

A link a member hands out is a marketing channel — usually the biggest one — so it is minted
as a `duncit.com/<code>` short link under a campaign, exactly like a link a marketer creates by
hand. `share-link.ts` is the contract both surfaces send: `SHARE_LINK_MUTATION` (a plain
string, because this package has no GraphQL client and must stay framework-free for Metro),
and `trackedShareUrl`, which asks once per thing and caches the answer for the session.

The surfaces never build the tracked URL. They say **what** is being shared — a target and the
id of the thing — and the server looks it up, builds the destination and returns the link. That
is what makes the mutation safe to leave unauthenticated: an id that resolves to nothing mints
nothing, so it can never be pointed somewhere we did not choose.

```ts
// mWeb: Apollo. The app: graphql-request. Same document, same answer.
const url = await shareUrl('POD', pod.id, `${origin}/club/${slug}/pod/${podSlug}`);
```

`plainUrl` is what the surface would have shared on its own, and it is what comes back when the
API cannot be reached. Sharing is the member's action, not the tracking's: a share sheet must
never fail to open because analytics did. A failed attempt is forgotten rather than cached, so
the next share retries.

`trackedPodShareLinks` resolves the **pair** a shared pod carries — the pod page and its venue
map — in one round trip. The map link is an external destination (Google Maps), and it is
counted like every other link: `buildPodShareMessage({ …, mapUrl })` takes the tracked one, and
falls back to the plain `podMapLink` search when it has none.

| Export | Signature | Meaning |
| --- | --- | --- |
| `SHARE_LINK_MUTATION` | `string` | The `shareLink(target, ref)` document, parsed by each surface's own client. |
| `ShareLinkTarget` | `'POD' \| 'POD_LOCATION' \| 'POD_FEEDBACK' \| 'POD_MEDIA' \| 'CLUB' \| 'PROFILE' \| 'POST' \| 'POD_IDEA' \| 'GIFT_CARD' \| 'REFERRAL'` | Mirrors the server enum. Each one has its own campaign. |
| `trackedShareUrl` | `(fetcher, target, ref, plainUrl) => Promise<string>` | The link to hand out; `plainUrl` when it cannot be minted. |
| `trackedPodShareLinks` | `(resolve, podId, plainUrl, plainMapUrl) => Promise<PodShareLinks>` | The pod link and its venue map link together. |
| `clearShareLinkCache` | `() => void` | Drops the session cache — sign-out and tests. |

## A pod's two links live here, not in the surfaces

A host hands out two addresses per pod — the rating form and the Upload Pod Media page — and
four things have to agree on each: the mWeb route, the native deep link, the short link the
server mints, and the message the host sends. So the path is written once.

```ts
podFeedbackPath('665f…')                    // '/pod/665f…/feedback'
podMediaPath('665f…')                       // '/pod/665f…/media'
podMediaLink('665f…', 'https://duncit.com') // the same, absolute, for a message that leaves the app
```

The copy follows the same rule as attendance's: `mwebPodMediaLabels(t)` for mWeb and native,
`shellPodMediaLabels(t)` for the portals, word-for-word identical values in two namespaces
because the server stores one Localization row per key path. Every key is a literal `t('…')`
call, because the key-verification gate greps source for the literal.

## Metro, Docker and the `dist/` build

The exports map points `import`/`types`/`react-native` at `src/index.ts` and only `require` at
`dist/index.js`. Metro compiles the TypeScript source directly — which means the native app
builds this package from source and the **whole directory** must be present in the image:

```dockerfile
COPY packages/utils ./packages/utils      # app/mobile-app/Dockerfile — NOT just package.json
```

The Vite portals need only `COPY packages/utils/package.json`, because pnpm resolves the
workspace link and Vite reads the source through it. Getting this backwards passes locally and
fails only in the deploy build.

## Leaderboard vocabulary — one list, two apps

`LEADERBOARD_CATEGORIES` is the five boards in display order (`USER`, `HOST`, `CLUB_ADMIN`,
`VENUE`, `BRAND`), and the companion maps carry each board's i18n keys and its pricing field so
mWeb and native render identical tabs, "how to earn" lines and period toggles from one
vocabulary (rule 27):

```ts
import {
  LEADERBOARD_CATEGORIES,
  LEADERBOARD_TAB_KEY,
  LEADERBOARD_EARN_KEY,
  LEADERBOARD_PERIOD_KEY,
  LEADERBOARD_POINTS_FIELD,
  leaderboardMedal,
} from '@duncit/utils';

LEADERBOARD_TAB_KEY.CLUB_ADMIN; // 'mweb.leaderboard.tabClubAdmin'
LEADERBOARD_POINTS_FIELD.BRAND; // 'points_per_product_sale' — the leaderboardConfig field
leaderboardMedal(1); // 'gold'  ·  leaderboardMedal(4) → null (past the podium)
```

The i18n keys are literals on purpose — the translation-key gate parses source statically and
cannot follow a key assembled from the category name.


## Badge vocabulary — the goal and the window it happens in

Every badge states two things a member has to read before it means anything: the GOAL
(`BADGE_GOAL_KEY`, whose copy takes the badge's threshold as `{target}`) and the WINDOW that
goal has to happen inside (`BADGE_WINDOW`, keyed to a label by `BADGE_WINDOW_KEY`). The window
follows from the CONDITION, never from an admin field — a monthly count resets with the
calendar wherever it is rendered, and no one can configure that apart:

```ts
import {
  BADGE_GOAL_KEY,
  BADGE_WINDOW,
  BADGE_WINDOW_KEY,
  badgeProgressPercent,
  sortBadgeProgress,
} from '@duncit/utils';

t(BADGE_GOAL_KEY.POD_ATTEND_COUNT, { vars: { target: 10 } }); // 'Attend 10 pods'
BADGE_WINDOW.MONTHLY_POD_ATTEND_COUNT;                        // 'CALENDAR_MONTH'
t(BADGE_WINDOW_KEY.CALENDAR_MONTH);        // 'Must all happen inside one calendar month'

badgeProgressPercent({ achieved: false, current: 4, target: 10 }); // 40
// An achieved badge always reads 100 — the metric can drift back below the
// threshold (a cancelled ticket) and a bar sliding backwards under an
// "Achieved" chip reads as a bug.
badgeProgressPercent({ achieved: true, current: 3, target: 10 }); // 100

sortBadgeProgress(rows); // earned first, then closest to unlocking
```

The placeholder is `{target}`, never `{count}`: the translator reserves `count` for its own
plural counter and blanks an unmatched one out of the sentence.
## The app-open popup — one dismissal, one set of dimensions

The marketing popup is rendered three times — MUI in mWeb, Tamagui in the native app and in
the native web build — so the two things that must never drift live here.

**Dismissal is a LIST OF POPUP IDS, never a `hasSeenPopup` flag.** Marketing publishes a new
campaign by creating a new id, and a single flag would silently swallow every campaign after
the first. The server also records a dismissal per user, but only after a round trip that can
fail: the local write is what hides the popup on the same frame and survives that failure, and
the server write is what carries it to that person's other devices.

```ts
import {
  readDismissedPopupIds,
  rememberDismissedPopup,
  isPopupDismissed,
} from '@duncit/utils';

// `storage` is described structurally, so each surface passes its own: `localStorage` in
// mWeb, `expo-secure-store` on a phone (which is `localStorage` again on native web).
const ids = await readDismissedPopupIds(storage);
isPopupDismissed(ids, 'popup-a'); // true  — closed on this device already
isPopupDismissed(ids, 'popup-b'); // false — new campaign, show it

await rememberDismissedPopup(storage, 'popup-b'); // ['popup-a', 'popup-b']
```

Storage can be unavailable — private browsing, a locked keystore — and every function above
absorbs that. The cost is the popup showing once more, never a throw on app open.

**Dimensions come from the artwork, not the screen.** Campaign images arrive in whatever
aspect the designer exported, so a fixed box letterboxes one and crops the next. The card is
sized to the picture instead, capped at `APP_POPUP_MAX_WIDTH` and at
`APP_POPUP_HEIGHT_FRACTION` of the viewport:

```ts
appPopupAspect({ width: 1080, height: 1350 }); // 0.8
appPopupAspect(null); // 0.8 — the portrait default, so the card cannot resize under the user

appPopupImageSize({ width: 1080, height: 1350 }, appPopupLimits({ width: 390, height: 844 }));
// { width: 342, height: 428 } — touches the width limit, well inside the height one
```

The native app calls `appPopupImageSize` against `useWindowDimensions`. mWeb expresses the
same rule as a CSS `min()` built from the same three constants, so a browser re-solves it on
every resize and orientation change without a listener.

`detectClientPlatform(userAgent)` answers which store build a BROWSER stands in for. Both
browser surfaces use it: a campaign aimed at Android has to reach an Android phone whether
the person opened mWeb or the native web build (rule 27).

## The @handle, and where a member may be messaged

Two things mWeb and the native app render identically (rule 27), so their LOGIC lives here
and only the MUI card and the Tamagui card differ (rule 40).

### The @handle is minted, then owned

A handle is minted ONCE by the server from the member’s name (`generateUsername` in
`server/src/modules/access/user/username.ts`, backfilled on `me` for accounts that predate
handles), and its owner may then change it from **Edit profile**. The field there is
debounced against `usernameAvailability` and the Save button is the profile’s own — which is
the whole reason `usernameBlocksSave` exists.

`usernameStatus` is the one place the field’s state is decided. Order matters: your OWN
handle reads as `CURRENT` rather than `TAKEN` (it IS taken — by you), and the shape is
checked before anything is sent, so typing a space never costs a round trip.

```ts
const idle = { current: 'ravi-9x3m', checking: false, available: null, reason: null };

usernameStatus({ ...idle, value: 'ravi-9x3m' });                        // 'CURRENT'
usernameStatus({ ...idle, value: 'Ravi Sharma' });                      // 'INVALID' — never sent
usernameStatus({ ...idle, value: 'ravi-plays' });                       // 'CHECKING'
usernameStatus({ ...idle, value: 'ravi-plays', available: true });      // 'AVAILABLE'
usernameStatus({ ...idle, value: 'admin', available: false, reason: 'RESERVED' }); // 'RESERVED'
```

`usernameBlocksSave(status, hasCurrent)` answers for the WHOLE form, because the handle
shares its Save with the name and the address beside it. `CHECKING` blocks too: the check is
400ms behind the last keystroke, so without it a fast typist reaches Save while the answer is
still in flight and the button reports the previous handle. An account with no handle yet is
the one case an empty field is fine — there is nothing to clear and nothing to break.

```ts
usernameBlocksSave('AVAILABLE', true); // false — the only handle that reaches the server
usernameBlocksSave('CHECKING', true);  // true  — the answer is still in flight
usernameBlocksSave('IDLE', true);      // true  — a profile cannot go back to no address
usernameBlocksSave('IDLE', false);     // false — pre-handle account, save the rest anyway
```

`scheduleUsernameCheck` owns the round trip itself — the 400ms debounce, the shape guard that
keeps a malformed handle on the device, and the cleanup that drops a reply for a value no
longer in the field. It returns that cleanup, which is exactly a React effect’s contract, so
each app supplies only its transport and its logger and neither owns the logic:

```ts
useEffect(
  () =>
    scheduleUsernameCheck({
      value,
      current,
      ask: (candidate) => client.query({ query: USERNAME_AVAILABILITY, variables: { username: candidate } })
        .then((r) => r.data.usernameAvailability),
      onState: setCheck,          // starts from IDLE_USERNAME_CHECK
      onError: (error, candidate) => logs.mWeb.error('useUsernameCheck', 'availability', { error, candidate }),
    }),
  [client, current, value],
);
```

A failed `ask` falls back to `IDLE_USERNAME_CHECK`, which `usernameStatus` reads as CHECKING —
telling somebody their handle is taken because the network blinked would be worse than making
them wait, and Save is disabled either way.

`usernameFieldState` is everything the field renders, derived once for both surfaces — the
MUI field and the Tamagui field disagree about how to draw a text box and agree about
absolutely everything else. Note the link: it previews the handle being TYPED once the server
has said yes, and otherwise the one that works today.

```ts
const settled = { checking: false, available: true, reason: null };
usernameFieldState({ value: 'ravi-plays', current: 'ravi-9x3m', check: settled, origin });
// { status: 'AVAILABLE', link: 'https://mweb.duncit.com/u/ravi-plays', errored: false }

usernameFieldState({ value: 'ravi-plays', current: 'ravi-9x3m', check: IDLE_USERNAME_CHECK, origin });
// { status: 'CHECKING', link: 'https://mweb.duncit.com/u/ravi-9x3m', errored: false }
```

The same pair is on the PROFILE, under the name and next to Share.

```ts
profileUrl('https://mweb.duncit.com', 'ravi-9x3m'); // 'https://mweb.duncit.com/u/ravi-9x3m'

const labels = buildUsernameLabels(t);
labels.handle('ravi-9x3m');                  // '@ravi-9x3m'
labels.status('AVAILABLE', 'ravi-plays');    // '@ravi-plays is available.'
labels.copyLink;                             // the button beside it
```

### Communication preferences

One row in Profile Settings opens a hub listing Email / WhatsApp / SMS; each of those opens
the screen that owns every switch for that channel, including its Authentication messages
switch. Nothing is settable from two places, so `commChannelSummary` (the hub’s one line per
channel) only ever reads.

```ts
const labels = buildCommPreferenceLabels(t);

commChannelSummary({ channel: 'EMAIL', reachable: true, destination: 'ravi@duncit.com', otp_enabled: true, otp_can_disable: true }, labels);
// 'ravi@duncit.com · Authentication messages on'
commChannelSummary({ channel: 'SMS', reachable: false, destination: '', otp_enabled: false, otp_can_disable: true }, labels);
// 'Add a phone number to get messages here.' — "off" and "no number" are different answers
```

`commRowState` answers what ONE channel’s switch may do, and `authMessageCardState` turns
that into the whole card the channel screen renders — title, body, the status line under it,
and whether a switch is offered at all:

```ts
commRowState({ channel: 'SMS', reachable: false, destination: '', otp_enabled: true, otp_can_disable: false });
// { canToggle: false, locked: false, unreachable: true } — nothing to send to, so no switch

commRowState({ channel: 'EMAIL', reachable: true, destination: 'ravi@duncit.com', otp_enabled: true, otp_can_disable: false });
// { canToggle: false, locked: true, unreachable: false } — on, and the last channel that can reach them
```

`locked` is the case worth naming: a disabled switch with no sentence beside it reads as a
bug, so `authMessageCardState` puts `labels.authLocked` in `note` whenever it is true. The
server decides `otp_can_disable`, because it is the only side that can see all three channels
at once — an account with nowhere to receive a code cannot sign in.

`findCommChannel(channels, channel)` is the one lookup both apps use, so a screen that wants
one channel out of the sheet never writes its own `.find`.

### Changing a contact detail

Email, phone and WhatsApp are the three profile fields whose change has to be PROVED, so
they are not fields of the edit-profile form at all. Each is a read-only row with its own
two-step dialog: type the new value, then type the code sent **to that new value**. mWeb
renders it in MUI and the native app in Tamagui; everything below is what they share.

`currentContactValue` is what the row displays, and it never shows a lone country code —
an account with an extension but no number has no number.

```ts
const account = {
  email: 'ravi@duncit.com',
  phone_extension: '+91',
  phone_number: '9845012345',
  whatsapp_extension: '+91',
  whatsapp_number: '',
};

currentContactValue(account, 'PHONE');    // '+91 9845012345'
currentContactValue(account, 'WHATSAPP'); // '' — the row falls back to "No WhatsApp number yet"
contactDraftFrom(account, 'PHONE');       // { email: '', extension: '+91', number: '9845012345' }
```

`contactDraftIsUnchanged` is the refusal that runs BEFORE a code is sent. A code costs the
person an SMS and a wait, so a draft matching what the account already holds is turned back
rather than delivered. For a phone the country code counts — `+1 9845012345` is a different
number from `+91 9845012345`, however alike the digits look:

```ts
contactDraftIsUnchanged(account, 'PHONE', { email: '', extension: '+91', number: '9845012345' });
// true — same number, no code sent
contactDraftIsUnchanged(account, 'PHONE', { email: '', extension: '+1', number: '9845012345' });
// false — same digits, different country

contactDraftValue({ email: 'Ravi@Duncit.com', extension: '', number: '' }, 'EMAIL');
// 'ravi@duncit.com' — what the server is asked to store
```

Nothing here validates a SHAPE. Each surface's dialog is a real form (rule 10), and the
patterns it checks live in `@duncit/regex`, which this zero-dependency package cannot
import.

### The follow button, and the follow rows in the inbox

Following a PRIVATE profile is a REQUEST, not a follow. The server is the only thing that
writes a follow edge; `followActionFor` / `nextFollowStatus` exist so the button predicts the
same outcome the server will produce, which is what makes an optimistic update safe.

```ts
followActionFor('REQUESTED');        // 'CANCEL_REQUEST' — there is no edge to unfollow yet
nextFollowStatus('NONE', true);      // 'REQUESTED' — a private profile is asked
nextFollowStatus('NONE', false);     // 'FOLLOWING'
```

The button's label knows the OTHER direction too. `PublicProfile.follows_viewer` says whether
this person already follows the viewer, and `followButtonLabelKey` turns the resting state
into "Follow Back" — the same words the inbox uses for the same tap, so a profile and the
notification about that person never disagree. Only NONE changes; REQUESTED and FOLLOWING
read as they always have.

```ts
followButtonLabelKey('NONE', true);       // 'mweb.follow.followBack' — they follow you, you don't follow them
followButtonLabelKey('NONE', false);      // 'mweb.follow.follow'
followButtonLabelKey('FOLLOWING', true);  // 'mweb.follow.following' — mutual, nothing to offer
```

A request is answered from two places — the inbox row, and the profile itself, which
carries `inbound_request_id` (the owner's OPEN ask against the viewer) so Accept / Deny can
sit above the button. Both act on the same FollowRequest; whichever the viewer reaches first
is the one that counts.

`followRequestRowState` is the same idea for the notification inbox. It takes a row exactly
as `myNotifications` returns it and says what that row offers:

```ts
followRequestRowState({ actionType: 'FOLLOW_REQUEST', requestId: 'fr-4821', status: 'PENDING' });
// 'ANSWER'      — Accept / Deny
followRequestRowState({ actionType: 'FOLLOW_REQUEST', requestId: 'fr-4821', status: 'ACCEPTED', followBackStatus: 'NONE' });
// 'FOLLOW_BACK' — 'Accepted', and an offer to follow them back
followRequestRowState({ actionType: 'FOLLOW_REQUEST', requestId: 'fr-4821', status: 'CANCELLED' });
// 'HIDDEN'      — withdrawn by the requester; the server deletes the row, and a stale one
//                 must not read as "Denied", an answer the viewer never gave
followRequestRowState({ actionType: 'NEW_FOLLOWER', actorId: 'u-aarav', followBackStatus: 'NONE' });
// 'FOLLOW_BACK' — no request behind it, so the button stands alone
followRequestRowState({ actionType: 'NEW_FOLLOWER', actorId: 'u-meera', followBackStatus: 'FOLLOWING' });
// 'HIDDEN'      — already followed back, so there is nothing to offer
```

`followOutcomeLabelKey` is the line a settled row states in place of Accept / Deny —
`'mweb.follow.accepted'`, `'mweb.follow.rejected'`, or null when there is nothing to state
(still open, withdrawn, or a NEW_FOLLOWER row).

`offersFollowBack` is the second, independent question the row asks: is there somebody here
the viewer does not follow yet? A→B and B→A are separate edges, so a request that is still
PENDING renders Accept / Deny **and** Follow Back — somebody asking to follow you is exactly
the moment you may want to follow them, and making that wait for an Accept is why people
reported having no way to do it. The rule is the simplest one that fits in a sentence: every
follow row offers it unless the viewer already follows that person.

```ts
const asking = { actionType: 'FOLLOW_REQUEST', requestId: 'fr-4821', status: 'PENDING', actorId: 'u-riya', followBackStatus: 'NONE' };
followRequestRowState(asking);  // 'ANSWER'  — Accept / Deny
offersFollowBack(asking);       // true      — ...and Follow Back beside them
offersFollowBack({ ...asking, status: 'DENIED' });               // true  — 'Denied', and the button
offersFollowBack({ ...asking, followBackStatus: 'FOLLOWING' });  // false — already followed
offersFollowBack({ ...asking, actorId: null });                  // false — nobody to act on
```

`NEW_FOLLOWER` matters more than it looks: a PUBLIC profile never receives a follow request,
so "X started following you" is the only follow row it ever gets. Without it there is no way
to follow anybody back from the inbox at all. Rows written before the actor column existed
carry no `actorId` and stay inert rather than rendering a button with nobody to act on.

`followBackLabelKey` and `canFollowBack` split the button's two live states: an open ask
reads "Requested" and stops accepting taps, because a pending request has nothing to re-send.

## An Auto Pod enrols in any order, and the first partner pins its city

Auto Pods used to be venue-first: only venues saw an `OPEN` offer, and a host or club admin was
offered it only after a venue had accepted. That is gone. A fresh offer is visible to all three
roles at once, and `autoPodActionable(row, role)` is true for any role whose own tick is still
empty while the offer is pre-live and the viewer has not enrolled. The venue › host › club order
in `AUTO_POD_ROLES` is how the chips are drawn, not a dependency — a card never changes width or
reorders as partners join, whichever went first.

What the first enrolment DOES fix is the city. An offer starts with `location: null`; the first
partner to enrol pins it to one admin Location row — a venue brings `venue.location_id`, a club
`club.location_id`, and a host the city selected at the top of their Auto Pods page. From then on
only partners in that city are offered it, and the server refuses a venue or club from anywhere
else. Two consequences a surface must render, and both come from here:

- `autoPodHostNeedsLocation(row, locationId)` — a host has no venue or club to bring a city with,
  so on an unpinned offer their "Assign Myself" is locked until they have selected one
  (`labels.pickLocationFirst`), and enrolling then pins it (`labels.willPinTo(city)`). On a
  pinned offer the selection is not consulted.
- `autoPodCityLabel(row.location)` — the card's city line, `labels.pinnedTo(city)` once pinned
  and `labels.unpinned` before, in exactly the same words on mWeb, native and every portal.

`autoPodMissingRoles(row)` feeds `labels.waitingFor(roles)` — "Waiting for a venue, a host" —
which replaced the single "waiting on" line, because with any-order enrolment there is no
single next partner to name.

## Adding a helper

`src/index.ts` is a pure re-export barrel and is coverage-excluded; everything else is under a
100/100/100/100 vitest gate. A new helper needs its branches tested before it will build, and it
must stay framework-free — no React, no MUI, no Tamagui, no Apollo. If a helper needs one of
those, it belongs in `@duncit/ui` (MUI) or the consuming app, not here.
