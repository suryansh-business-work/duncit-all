# Loaded by `vite build --mode e2e` (via `pnpm build:e2e`). Forces the bundle
# to talk to a same-origin `/graphql` so Cypress's `cy.intercept()` always
# catches the request — no cross-origin CORS preflight to the real server.
VITE_GRAPHQL_URL=/graphql
