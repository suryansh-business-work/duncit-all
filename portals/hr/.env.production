# Local production-target build (`vite build --mode production`).
# CI overrides these with build-args; secrets stay blank here.
VITE_GRAPHQL_URL=https://server.duncit.com/graphql
VITE_REQUIRED_ROLES=HR_MANAGER
