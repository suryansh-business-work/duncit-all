/**
 * The multi-image list field — the shared one.
 *
 * This console kept a hand-copy of @duncit/media-picker's, which is exactly the
 * drift rule 40 exists to stop: the package's version had gained the AI
 * Monitoring notice every upload field is supposed to carry (rule: the notice
 * sits beside the upload, not beside some of them) and this copy had not.
 * Re-exported rather than re-implemented, so the next change lands in both.
 */
export { MediaListField as default } from '@duncit/media-picker';
