/**
 * Theme — delegated to the shared @duncit/theme design system. The brand accent
 * (in config/app-config) is the only per-portal value; all colours, fonts,
 * sizes and component styles live in @duncit/theme.
 */
export { buildTheme, tokens } from '@duncit/theme';
