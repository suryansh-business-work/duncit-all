---
import { siteContent } from '../config/site-config';

const siteConfig = await siteContent();
const { features, featuresSection } = siteConfig;
---

<section id="features" class="mx-auto max-w-6xl scroll-mt-20 px-5 py-16">
  <div class="mx-auto max-w-2xl text-center">
    <h2 class="text-3xl text-ink sm:text-4xl">{featuresSection.heading}</h2>
    <p class="mt-4 text-ink-soft">{featuresSection.text}</p>
  </div>
  <div class="mt-12 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">
    {
      features.map((feature) => (
        <article class="rounded-2xl border border-line bg-surface p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-md">
          <span class="flex h-12 w-12 items-center justify-center rounded-xl bg-primary-light text-primary-dark">
            <i class={`fa-solid ${feature.icon} text-xl`} aria-hidden="true"></i>
          </span>
          <h3 class="mt-5 text-lg text-ink">{feature.title}</h3>
          <p class="mt-2 text-sm leading-relaxed text-ink-soft">{feature.text}</p>
        </article>
      ))
    }
  </div>
</section>
